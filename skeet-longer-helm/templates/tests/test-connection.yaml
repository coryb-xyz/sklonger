apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "skeet-longer.fullname" . }}-test-connection"
  labels:
    {{- include "skeet-longer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "skeet-longer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
