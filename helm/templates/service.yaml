apiVersion: v1
kind: Service
metadata:
  name: {{ include "sklonger.fullname" . }}
  labels:
    {{- include "sklonger.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sklonger.selectorLabels" . | nindent 4 }}
